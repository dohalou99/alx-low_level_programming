#include "main.h"
		
/**
 * _isdigit - a function checks for a digit (0 through 9)
 * @c: the input cheked int
 * Return: 1 if c is a digit, 0 if it is not
 */
int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}

