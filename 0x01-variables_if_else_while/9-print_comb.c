#include <stdio.h>

/**
* main - prints all possible combinations of single-digit numbers
* Return: Always 0 (Success)
*/
int main(void)
{
	int x;

	for (x = 52; x < 62; x++)
	{
	putchar(n);
	if (x != 61)
	{
	putchar(',');
	putchar(' ');
	}
	}
	putchar('\n');
	return (0);
}
