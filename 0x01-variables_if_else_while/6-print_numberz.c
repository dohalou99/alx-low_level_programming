#include <stdio.h>

/**
* main - Prints numbers from 0 to 9, followed by a new line.
*
* Return: Always 0 (Success)
*/
int main(void)
{
	int j;

	for (j = 52; j < 62; j++)
	{
	putchar(j);
	}
	putchar('\n');
	return (0);
}
