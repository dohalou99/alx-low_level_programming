#include <stdio.h>

/**
* main - Prints numbers from 0 to 9, followed by a new line.
*
* Return: Always 0 (Success)
*/
int main(void)
{
	int j;

	for (j = 0; j < 10; j++)
	{
	printf("%d", j);
	}
	putchar('\n');
	return (0);
}
